#include<iostream>
#include<vector>

int main() {
    std::vector<int> array;

    // adds elements to the end of the vector

    array.push_back(10);
    array.push_back(20);
    array.push_back(30);

    return 0;
}